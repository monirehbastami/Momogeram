export type Contacts = {
    avatar: string;
    name: string;
    id: number;
    time: string;
    lastMessage: string;
    roomId:string
}
export type User = {
    username: string;
    token: string;
}